{
  "name": "@memory-river/benchmark",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "mr-bench": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "probe:locomo-reranker": "node dist/harness/locomo-reranker-probe.js",
    "test": "node --test tests/*.test.mjs dist/dimensions/__tests__/*.test.js"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "onnxruntime-node": "1.14.0"
  },
  "devDependencies": {
    "@memory-river/core": "0.1.0"
  }
}
